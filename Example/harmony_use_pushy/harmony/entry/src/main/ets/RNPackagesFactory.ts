import type {RNPackageContext, RNPackage} from 'rnoh/ts';
// import {GeoLocationPackage} from 'geolocation/ts';

export function createRNPackages(ctx: RNPackageContext): RNPackage[] {
  return [];
  // return [new GeoLocationPackage(ctx)];
}
